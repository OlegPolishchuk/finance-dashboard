import React from 'react';

import { SignUpForm } from '@/app/(pages)/(auth)/components/SignUpForm';

const Page = () => {
  return (
    <main className='flex min-h-screen items-center justify-center'>
      <SignUpForm />
    </main>
  );
};

export default Page;
